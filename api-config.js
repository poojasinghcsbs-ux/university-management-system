// Change this one value after the backend is deployed.
// Keep the trailing "/api" and do not add a slash at the end.
window.UNIVERSITY_PORTAL_API_URL =
    window.UNIVERSITY_PORTAL_API_URL || "http://localhost:8080/api";
